# TraceSketch_Rectangle

Second unfamiliar program from the lesson.

Expected result: a gray square with a 2-pixel black outline moves upward by 3 pixels and shrinks by 1 pixel per `draw()` call. The background clears every frame, so there is no trail.

First visible values: `y = 177`, `w = 59`. After three calls: `y = 171`, `w = 57`.
