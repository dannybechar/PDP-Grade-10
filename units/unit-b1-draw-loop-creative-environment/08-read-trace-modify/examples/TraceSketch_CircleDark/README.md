# TraceSketch_CircleDark

Deliberate local-style modification of `TraceSketch_Circle`.

Expected result: the same movement, growth, and no-trail behavior, but the circle is darker because local `shade` is 80 instead of 180. The `x` and `d` traces are unchanged.
