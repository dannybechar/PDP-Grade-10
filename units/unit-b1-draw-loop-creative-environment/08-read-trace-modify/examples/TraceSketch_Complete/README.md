# TraceSketch_Complete

Verified field modification of `TraceSketch_Rectangle`.

Expected result: the same gray outlined square moves upward by 6 pixels per `draw()` call—twice the original vertical displacement—while still shrinking by 1 pixel. Direction, starting values, width change, fill, outline, and no-trail behavior remain unchanged.

First visible values: `y = 174`, `w = 59`. After three calls: `y = 162`, `w = 57`.
