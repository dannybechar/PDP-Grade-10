# VelocitySketch_Left

Demonstrates signed horizontal velocity.

Expected result: one orange circle moves left by 3 pixels per frame because `vx` is negative. `x` starts near the right side so the direction is easy to observe.
