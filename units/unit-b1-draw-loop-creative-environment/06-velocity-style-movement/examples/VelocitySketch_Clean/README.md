# VelocitySketch_Clean

The clean-motion comparison for the trail example.

Expected result: one orange circle moves right by 4 pixels per frame. `background(255)` runs first in `draw()`, so only the newest position remains visible.
