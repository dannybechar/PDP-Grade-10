# VelocitySketch_Trail

Shows cumulative drawing when a moving shape is not cleared on every frame.

Expected result: an orange trail builds from left to right. `background(255)` runs only once in `setup()`, so every old circle remains visible.
