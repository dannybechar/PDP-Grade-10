# VelocitySketch_Starter

The verified shell used at the start of B1.3.2.

Expected result: a blank white 400 × 300 Processing window. Add the persistent position, signed per-frame velocity, update, and `ellipse()` call during the lesson.
