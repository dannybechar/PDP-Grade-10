# VelocitySketch_Vertical

Applies the same movement pattern to the screen y-axis.

Expected result: one blue circle moves upward by 2 pixels per frame. `vy` is negative, so the screen y-coordinate decreases.
