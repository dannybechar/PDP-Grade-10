# VelocitySketch_Complete

The completed construction target for B1.3.2.

Expected result: one orange circle moves right by 2 pixels per frame with no trail. The first visible center is `x = 32` because the update runs before `ellipse()`.
