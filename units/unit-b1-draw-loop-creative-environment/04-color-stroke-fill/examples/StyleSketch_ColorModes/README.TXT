STYLE SKETCH — GRAYSCALE AND RGB

Purpose
Compare one grayscale value with three RGB values in Processing's default colour mode.

Expected
The left rectangle is gray. The right rectangle is a warm reddish-brown because red is the strongest channel.

Range
The lesson uses the default 0–255 range for each value.
