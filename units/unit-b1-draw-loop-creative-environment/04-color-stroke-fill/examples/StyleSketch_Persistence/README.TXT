STYLE SKETCH — PERSISTENCE

Purpose
Checkpoint 2: prove that a style call stays active until another style call changes it.

Expected
The first ellipse and rectangle share fill(220). The final ellipse uses fill(80).

Explain
Why does the rectangle use fill(220) even though it does not call fill() itself?
