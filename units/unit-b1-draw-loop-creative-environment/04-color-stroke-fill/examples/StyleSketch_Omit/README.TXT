STYLE SKETCH — NO FILL AND NO STROKE

Purpose
Compare noFill() with noStroke() and show how a later style call restores a disabled part.

Expected
The rectangle is outline-only. The ellipse is fill-only.

Watch
If noFill() and noStroke() are both active, the next shape will be invisible.
