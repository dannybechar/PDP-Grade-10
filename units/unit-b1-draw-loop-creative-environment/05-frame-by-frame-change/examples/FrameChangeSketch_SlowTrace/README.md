# FrameChangeSketch_SlowTrace

The sketch runs at one frame per second and prints the current diameter.

Expected result: the terminal reports `d = 21.0`, `d = 22.0`, `d = 23.0`, and so on while the circle grows slowly.
