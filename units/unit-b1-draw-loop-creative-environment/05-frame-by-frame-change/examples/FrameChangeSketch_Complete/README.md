# FrameChangeSketch_Complete

The complete construction target from slide 12.

Expected result: an opaque orange circle stays centered and grows by 2 pixels per frame. The background is set once; each larger circle covers the smaller circle drawn before it.
