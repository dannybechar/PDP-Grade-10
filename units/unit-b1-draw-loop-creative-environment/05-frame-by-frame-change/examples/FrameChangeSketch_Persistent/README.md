# FrameChangeSketch_Persistent

The diameter is a field declared once in the class and updated inside `draw()`.

Expected result: the centered circle grows by 4 pixels per frame. `background(30)` clears the previous picture before the new size is drawn.
