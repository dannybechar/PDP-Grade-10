# CombinedSketch_Complete

Complete construction target for slide 12.

Expected result: one orange circle moves right by 2 pixels and grows by 1 pixel in diameter per `draw()` call. The first visible values are `x = 32` and `d = 21`.
