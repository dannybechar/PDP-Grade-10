# CombinedSketch_MoveOnly

Retrieval example from B1.3.2.

Expected result: one orange circle moves right by 3 pixels per `draw()` call and stays 40 pixels in diameter. The first visible centre is `x = 33`.
