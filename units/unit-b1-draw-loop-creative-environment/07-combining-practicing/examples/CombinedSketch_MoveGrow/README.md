# CombinedSketch_MoveGrow

Core combination example.

Expected result: one orange circle moves right by 3 pixels and grows by 1 pixel in diameter per `draw()` call. The first visible values are `x = 33` and `d = 21`.
