# PropertySketch_BalloonComplete

Three proportionally scaled balloons at different positions.

Compile PropertySketch in BlueJ, run void main(String[] args), and compare the window with this description.
