# ComponentSketch_BalloonStarter

Student starting point for the construct task.

Expected result: a gray 60-pixel circle centered at `(200, 150)` and a black 6 × 80 string beginning at `(197, 180)` on a white 400 × 400 canvas.

Task: keep `background(255)` in `draw()`, move the four balloon drawing statements into `drawBalloon()`, and call the new method once from `draw()`.

