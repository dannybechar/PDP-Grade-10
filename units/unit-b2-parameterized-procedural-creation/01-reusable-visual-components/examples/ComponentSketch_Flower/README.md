# ComponentSketch_Flower

The flower drawing statements are packaged into `drawFlower()` and called once from `draw()`.

Expected result: visually identical to `ComponentSketch_Inline`. The code organisation changes, but the order and effect of the drawing statements do not.

