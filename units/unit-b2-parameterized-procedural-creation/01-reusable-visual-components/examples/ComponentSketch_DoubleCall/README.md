# ComponentSketch_DoubleCall

Prediction project for two calls to the same fixed-coordinate method.

Expected result: only one flower is visible. Both calls run, but both flowers use the same coordinates, so the second is drawn directly over the first.

