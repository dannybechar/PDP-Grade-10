# RandomSketch_Position

Six fixed-size circles at random in-bounds centres; each run creates a new arrangement.

Compile RandomSketch in BlueJ, run void main(String[] args), and compare the window with this description.
