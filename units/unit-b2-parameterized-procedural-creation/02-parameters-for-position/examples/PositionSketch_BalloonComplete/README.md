# PositionSketch_BalloonComplete

Three balloons; each string preserves its offset from the supplied centre.

Compile PositionSketch in BlueJ, run void main(String[] args), and compare the window with this description.
