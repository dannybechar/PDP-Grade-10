# Scenes_Model

Open this folder as a BlueJ project, compile `InteractionSketch`, and run `main(String[] args)`. A 400 × 400 Processing window should open. Test the mouse and/or keyboard interaction described by the code and compare it with the lesson prediction.
